function config = user_config()
    % USER_CONFIG User-specific configuration overrides
    %
    % Copy this file to user_config.m and modify as needed.
    % Only include settings you want to override.
    
    % Example: Override database settings
    % config.database.server = 'my-server.local';
    % config.database.database = 'NARWC_Test';
    
    % Example: Override validation settings
    % config.validation.coordinates.study_area_lat = [25, 50];
    
    % Example: Change logging level
    % config.logging.level = 'DEBUG';
    
    config = struct();  % Empty = no overrides
end